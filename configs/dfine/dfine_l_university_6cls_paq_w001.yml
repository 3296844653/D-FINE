# Deprecated gate-ablation config from the previous PaQ-style module.
# Strict PaQ has no residual gate; this now runs the same PaQ query as paq.yml.

__include__:
  - dfine_l_university_6cls.yml

output_dir: ./output/dfine_l_university_6cls_paq_w001

DFINETransformer:
  use_paq_query: True
  paq_num_patterns: 100

  # Keep BCQS disabled so this run measures strict PaQ alone.
  use_bcqs: False
