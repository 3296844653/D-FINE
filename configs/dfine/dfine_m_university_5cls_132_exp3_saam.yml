__include__:
  - dfine_m_university_5cls_132.yml

# Exp-3 (single variable): H3Former SAAM V->E->V hypergraph propagation
# adapted to final D-FINE decoder queries; box regression remains unchanged.
DFINETransformer:
  use_saam_refine: true
  saam_num_hyperedges: 16
  saam_key_dim: 32
  saam_dropout: 0.0
  saam_residual_init: 0.05
