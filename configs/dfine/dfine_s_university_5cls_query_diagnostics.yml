# Validation-only query export. Run with --test-only -r <checkpoint>.
# This does not change the model, losses, training settings, or COCO metrics.
__include__:
  - dfine_s_university_5cls.yml

export_query_diagnostics: true
query_diagnostic_conf_thresh: 0.5
query_diagnostic_iou_thresh: 0.5
query_diagnostic_neighbor_iou_thresh: 0.8
